# .env.example

# MongoDB configuration
DB_USER=your_email@example.com
DB_PASS=your_mongo_password
DB_CLUSTER=vibe-test.jt5wz7s
DB_NAME=vibe

# Optionally, you can use a full connection string instead of the above variables:
# DB_URL="mongodb+srv://your_email@example.com:your_mongo_password@vibe-test.jt5wz7s.mongodb.net/vibe?retryWrites=true&w=majority&appName=vibe-test"

# Other environment variables
NODE_ENV=development
PORT=4001

# Firebase Admin SDK (if used)
# GOOGLE_APPLICATION_CREDENTIALS=path/to/your/firebase/credentials.json